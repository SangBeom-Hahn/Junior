package HSB;

public class main { //조건문, 반복분 return 조건, 문자열에서 문자빼는 라이브러리 사용기
	public static void main(String[] args) {
		
		BST<Integer, String> b = new BST<>();
		b.root = b.map(); // n1보자
		//String val = b.get(b.root, "n5"); 왜인지 안됨
	}
}